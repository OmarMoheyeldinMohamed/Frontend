name: Formatting

on:
  push:
  pull_request:

jobs:
  check-license-headers:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Check license headers
      run: |
        for file in $(git ls-files | grep -E '\.(h|c)$'); do
          grep "Hi" $file
        done